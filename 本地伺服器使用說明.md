# 專案管理與 RFI 追蹤系統 — 本機電腦版使用說明手冊

歡迎使用**專案管理與 RFI 資訊需求單追蹤系統（本機電腦型版本）**。本系統已完成本機環境適配與一鍵起停控制工具，讓您可以在 Windows 電腦上輕鬆作為獨立本機軟體使用。

---

## 🚀 快速開始（一鍵雙擊）

專案根目錄已為您建立好專為 Windows 設計的批次檔，**無需打開終端機手動輸入指令**：

### 1. 啟動伺服器
* **操作方式**：滑鼠雙擊專案目錄下的 **`啟動伺服器.bat`**（或 `start-server.bat`）。
* **自動執行流程**：
  1. 自動檢查系統是否安裝 Node.js。
  2. 若初次運行，會自動執行 `npm install` 安裝所需套件。
  3. 自動關閉可能佔用 Port 3000 的舊行程。
  4. 於獨立的專屬視窗啟動伺服器。
  5. **自動在您的預設瀏覽器中開啟** `http://localhost:3000`。

### 2. 關閉伺服器
* **操作方式**：滑鼠雙擊專案目錄下的 **`關閉伺服器.bat`**（或 `stop-server.bat`）。
* **自動執行流程**：
  1. 依據 `.server.pid` 行程識別碼安全終止伺服器。
  2. 自動搜尋並釋放所有佔用 Port 3000 的行程。
  3. 顯示綠色「已成功停止」訊息，乾淨關閉，不留任何背景佔用。

---

## 💾 本機資料保存與備份管理

本機電腦版本已內建**完整資料持久化機制**：

1. **自動即時儲存（LocalStorage）**：
   * 所有新增/修改的看板、卡片工單、RFI 需求單、時程與通知，皆會即時保存於您電腦本機瀏覽器存儲空間中。
   * **重新整理網頁、關閉瀏覽器或重啟伺服器，資料都不會消失**。

2. **匯出 JSON 備份檔**：
   * 點擊頂部導航列右側的 **「本機資料」** 按鈕。
   * 點選 **「匯出 JSON 備份檔」**，系統將自動下載如 `kanban-rfi-backup-2026-09-11.json` 檔案。
   * 適合定期存檔或轉移到其他電腦使用。

3. **匯入 JSON 備份檔**：
   * 於「本機資料」彈窗中點選 **「匯入 JSON 備份檔」**，選取您先前的 JSON 檔即可瞬間還原。

4. **還原出廠範例資料**：
   * 若測試完畢想還原初始乾淨範例，點選 **「還原為系統初始範例」** 即可一鍵重設。

---

## 🛠️ 開發者與命令列指令（Terminal）

若您習慣使用 PowerShell、CMD 或 VS Code 終端機，亦可直接使用以下指令：

| 指令 | 說明 |
| :--- | :--- |
| `npm run dev` | 啟動 Vite 開發伺服器（支援熱模組即時更新 HMR） |
| `npm run build` | 編譯靜態資源至 `dist/` 目錄 |
| `npm run preview` | 預覽編譯後的生產版本 |
| `npm run server` 或 `npm start` | 啟動 Express 本機生產伺服器（託管 `dist/` 並提供 `/api/health`） |
| `npm run clean` | 跨平台清理 `dist/` 與伺服器 PID 記錄 |
| `.\start-server.ps1` | PowerShell 專用一鍵啟動腳本 |
| `.\stop-server.ps1` | PowerShell 專用一鍵關閉腳本 |

---

## ❓ 常見問題排解 (FAQ)

### Q1：雙擊 `啟動伺服器.bat` 顯示「未安裝 Node.js」？
* **解答**：請前往 [Node.js 官方網站](https://nodejs.org/) 下載安裝 LTS 版本（建議 Node 18 以上，本專案在 Node 24 測試通過）。安裝後重新雙擊 `啟動伺服器.bat` 即可。

### Q2：如何修改預設的通訊埠（Port 3000）？
* **解答**：打開專案目錄下的 `.env.local` 檔案，修改 `PORT=3000` 為您想要的埠號（例如 `PORT=8080`），存檔後重啟伺服器即可。

### Q3：同一局域網的其他電腦或手機可以連線嗎？
* **解答**：可以！伺服器預設綁定 `0.0.0.0`。您只要在同網段的手機或另一台電腦瀏覽器輸入 `http://<您的電腦IP>:3000` 即可訪問。

### Q4：伺服器如何確認是否正常運行？
* **解答**：瀏覽器訪問 `http://localhost:3000/api/health`，若伺服器正常將會回傳包含運行時間（uptime）、系統狀態與行程 PID 之 JSON 資訊。

# AI 代理作業規範與開發指南 (AGENTS.md)

本文件定義所有 AI 代理（包括 Antigravity、Gemini、GitHub Copilot、Cursor 等）在此專案中的核心行為準則、開發流程與程式碼審查規範。

---

## 1. 核心開發流程與規範（強制執行）

在此專案進行任何新功能開發或重大重構時，**必須無條件遵循以下四步驟生命週期**：

```
[步驟 1: 需求討論] 
       ↓ 
[步驟 2: 撰寫計畫 (doc/dev/plan_<功能名稱>.md)] 
       ↓ 
[步驟 3: 用戶確認與執行開發] 
       ↓ 
[步驟 4: 規格回寫與同步 (doc/spec/)]
```

### 步驟 1：開發前討論 (Discuss First)
- 在變更任何程式碼之前，代理必須先向使用者釐清需求細節、邊界條件、影響模組與資料流。
- 嚴禁未經討論即逕行撰寫大規模程式碼。

### 步驟 2：擬定開發計畫 (Draft Dev Plan)
- 討論完成後，於 `doc/dev/` 目錄建立開發計畫文件。
- **命名規則**：`doc/dev/plan_<功能名稱>.md`（例如：`plan_看板自訂欄位排序.md`、`plan_rfi匯出pdf報表.md`）。
- **內容結構**：
  - 功能目標與背景
  - 影響的模組與檔案（包含型別變更）
  - 實作步驟與細部檢查清單
  - 測試與驗證計畫
  - 潛在風險與防範措施
- 開發過程中，代理**必須持續參照並更新此計畫進度**。

### 步驟 3：用戶確認與執行開發 (Confirm & Execute)
- 將計畫呈現給使用者並獲得確認後，開始實施程式碼修改。
- 遵循最小修改原則，保持程式碼簡潔與原有架構一致。

### 步驟 4：規格回寫 (Write Back to Spec)
- 功能開發完成且驗證通過後，**必須將最終規格寫回 `doc/spec/` 目錄**。
- **分章節撰寫**：`doc/spec/` 採章節式組織（如 `01_系統架構.md`, `02_看板與任務卡片模組.md` 等），若新功能擴充了既有模組，需更新對應章節；若屬全新獨立系統，則新增後續章節。
- 確保 `doc/spec/` 永遠是專案最終正確真實狀態 (Single Source of Truth) 的依據。

---

## 2. 程式碼審查準則 (Code Reviewer Standards)

身為 Code Reviewer，代理應如同導師般提供具體、可落實、聚焦於本質的審查建議：

### 🎯 審查五大維度
1. **正確性 (Correctness)**：程式碼是否完整達成規格？邊界條件、空值與非同步競爭條件是否處理？
2. **安全性 (Security)**：是否存在 XSS、腳本注入、敏感資料洩漏或不安全的 DOM 操作？（特別是即時 HTML 編輯器需維持 iframe 沙盒隔離）
3. **可維護性 (Maintainability)**：模組職責是否清晰？型別定義是否完備？命名是否語意明確？
4. **效能 (Performance)**：是否存在不必要的重複渲染、記憶體洩漏、超大陣列無分頁遍歷？
5. **測試性 (Testing)**：關鍵路徑與極端情境是否具備驗證手段？

### 優先級標記法
- 🔴 **Blocker (必須修復)**：安全性漏洞、資料遺失風險、嚴重視覺損壞或破壞性 API 變更。
- 🟡 **Suggestion (建議改進)**：缺少輸入防護、命名含糊、型別鬆散、可萃取的重複邏輯。
- 💭 **Nit (輕微優化)**：微小的註解增補、語法糖建議、潛在的風格統一。

---

## 3. 技術棧慣例與開發約束

1. **React 19 與 Hooks**：
   - 遵循 React 19 最新標準，狀態集中於主應用或透過狀態提升管理。
   - 避免直接操作原生 DOM，嚴格透過 React State / Ref 進行驅動。
2. **TypeScript 嚴格型別**：
   - 核心實體型別置於 [`src/types.ts`](file:///c:/%E6%96%B0%E5%A2%9E%E8%B3%87%E6%96%99%E5%A4%BE%20%282%29/2026260911.coding-sample/src/types.ts)。
   - 禁止濫用 `any`；若有新實體或欄位，必須先更新 `types.ts`。
3. **Tailwind CSS v4**：
   - 保持現代深色工程風格，依據 [`DESIGN.md`](file:///c:/%E6%96%B0%E5%A2%9E%E8%B3%87%E6%96%99%E5%A4%BE%20%282%29/2026260911.coding-sample/DESIGN.md) 中規範之語彙與配色使用。
4. **文件完整性維持**：
   - 保留現有檔案中的註解、型別說明與 JSDoc。
   - 每次重大異動務必同步檢查 `metadata.json` 與相關文件。
